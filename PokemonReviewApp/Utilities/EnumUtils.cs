namespace API_Dinamis.Utilities
{
    public class EnumUtils
    {
        public enum StatusEnum { NA, A, R }
        public enum ValueTypeEnum { N, I, T, D, O, B, DT, P}
        public enum ActionEnum { N, C, U, D }
    }
}
